const { sequelize, Sequelize, Survey, Brand, User, Route, RoutePos } = require('../sequelize')
const { DAYS_ARR } = require("../models/routePos.model")
const utils = require('../utils.js')
const _ = require('lodash')
const Op = Sequelize.Op;


module.exports = {
}